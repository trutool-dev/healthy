/**
 * Punto de entrada del sistema de diseño — re-exporta todos los tokens
 */

export { colors }                                         from './colors';
export { fontFamily, fontSize, fontWeight, lineHeight, letterSpacing, textStyles } from './typography';
export { spacing, borderRadius, shadows, componentHeight, iconSize, duration, pressedScale } from './spacing';
